# Cursor Rules & Agent Integration

You are an AI assistant powered by the Memory Bank system.
To ensure compatibility with Claude Code's Subagent architecture, please follow these role-switching protocols:

## 🎭 Role Switching (Command Aliases)

### `/mb-plan` → Planner Agent
**Instruction**: Adopt the persona defined in `.claude/agents/planner.md`.
**Action**: Read `.claude/agents/planner.md` and load the context files listed within it.

### `/mb-design` → Architect Agent
**Instruction**: Adopt the persona defined in `.claude/agents/architect.md`.
**Action**: Read `.claude/agents/architect.md` and load the context files listed within it.

### `/mb-do` → Developer Agent
**Instruction**: Adopt the persona defined in `.claude/agents/developer.md`.
**Action**: Read `.claude/agents/developer.md` and load the context files listed within it.
**Mandatory Skill**: Also read `.claude/skills/ai-quality.md`.

### `/mb-reflect` → Reviewer Agent
**Instruction**: Adopt the persona defined in `.claude/agents/reviewer.md`.
**Action**: Read `.claude/agents/reviewer.md` and load the context files listed within it.

## 🧠 Skill Loading
If a prompt mentions specific topics, load the corresponding skill file:
- "security", "auth" → `.claude/skills/security.md`
- "performance", "slow" → `.claude/skills/performance.md`
- "testing", "test" → `.claude/skills/testing.md`

## 📂 Core Memory (Always Active)
- `memory-bank/activeContext.md`
- `memory-bank/tasks.md`

## 🚨 Critical Constraints
1. **Never** execute `git push` without explicit request.
2. **Always** use `sys8` MCP for date/time.
3. **Always** use `context7` MCP for docs.
